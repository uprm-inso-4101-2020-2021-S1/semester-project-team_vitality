version: "3.7"

services:
    app:
        build: .
        ports:
            - 5000:5000
        volumes:
            - ./:/app
        environment:
            FLASK_APP: app.py
            FLASK_ENV: development